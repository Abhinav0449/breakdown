import React from 'react'
import load from '../images/load.gif'

const Loading = () => {
    return (
        <img src={load} alt="<h1>LOADING</h1>" className='loading'/>
    )
}

export default Loading
